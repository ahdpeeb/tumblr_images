# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

use_frameworks!
inhibit_all_warnings!

target 'tumblr_images' do
    pod 'Alamofire', '~> 4.7.0'
    pod 'ObjectMapper', '~> 3.1.0'
    pod 'SDWebImage', '~> 4.0'
end
